/** Telos KB specific implementation of the Type inteface. Really a type just
 * a wrapper for a telos proposition. */

package OME;

class TelosType implements OMEType {
    /*
    Proposition type_prop;

    public TelosType (Proposition type) {
	this.type = type;
    }
    */
}
